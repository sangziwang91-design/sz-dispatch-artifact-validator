/**
 * Public, sanitized successor interface for the private
 * `sz-dispatch-validator-v0.2.tsx` experiment.
 *
 * The implementation is intentionally provider-neutral and delegates to the
 * v0.3 evidence model. The private original is not distributed.
 */
export { default } from "../components/DispatchCapabilityValidator.jsx";
